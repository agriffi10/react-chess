import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom';

describe('Game Over Screen', () => {
  describe('Given I am playing a game of chess, when the game has ended, then correct game over screen is displayed', () => {});
});
